name: ClodMC
version: '${version}'
main: au.com.glob.clodmc.ClodMC
bootstrapper: au.com.glob.clodmc.modules.ModuleRegistry
api-version: '1.21.6'
authors: [ glob ]
dependencies:
  bootstrap:
    GriefPrevention:
      load: BEFORE
      required: false
  server:
    GriefPrevention:
      load: BEFORE
      required: false
    BlueMap:
      load: BEFORE
      required: false
